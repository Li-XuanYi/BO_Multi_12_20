You are an expert in lithium-ion battery fast charging optimization.

Task:
[TASK_BRIEF]

Battery and charging window:
- Cell: [BATTERY_NAME]
- Parameter set: [PARAM_SET_DISPLAY]
- SOC window: [SOC_START] to [SOC_END]

Decision variables and bounds:
- I1 (stage-1 current): [I1_RANGE]
- I2 (stage-2 current): [I2_RANGE]
- I3 (stage-3 current): [I3_RANGE]
- dSOC1 (stage-1 SOC span): [DSOC1_RANGE]
- dSOC2 (stage-2 SOC span): [DSOC2_RANGE]

Constraints:
- Practical hard limit: keep dSOC1 + dSOC2 strictly below [DSOC_SUM_MAX] because points at exactly [DSOC_SUM_MAX] can be rejected by the simulator.
- Safety margin: keep dSOC1 + dSOC2 <= [SAFE_DSOC_SUM_MAX] whenever possible.
- Physical risk: [SAFE_MARGIN_RISK_TEXT]
- A decreasing current profile I1 >= I2 >= I3 is recommended.

Optimization objectives:
[OBJECTIVE_SUMMARY]

Problem details:
[PROBLEM_DETAIL]

Warm-start set objective:
- [COLLECTION_OBJECTIVE]

Trade-off buckets to cover:
[TRADEOFF_BUCKETS]

Coverage rules:
- At least [PER_BUCKET_MIN_COUNT] protocol per trade-off bucket before adding a second protocol to any bucket when the requested count allows it.
[ANTI_COLLAPSE_RULES]

Apply expert knowledge when proposing initial warm-start protocols:
[EXPERT_KNOWLEDGE]

[FEW_SHOT_BLOCK]

[NEGATIVE_EXAMPLE_BLOCK]

Anti-patterns to avoid:
[ANTI_PATTERNS]

Generate [NUM_RECOMMENDATION] warm-start protocols for a multi-objective Bayesian optimization workflow. Favor precise, non-rounded values when possible, maximize initial Pareto coverage, and avoid collapsing all proposals to a single trade-off region.
Respond with ONLY a JSON array and no extra text.
[OUTPUT_SCHEMA]

% Ecker2015 final normalized hypervolume (NHV) values.
% Sources: final Pareto fronts for seeds 8409--8413 under the 56-evaluation budget.
% Common raw ideal/reference points for both methods:
%   z_ideal = [2500, 0, 0.2], r_HV = [7000, 35, 4.5].
% Time and aging are transformed with log10 before normalization.
% Each normalized objective is clipped to [0,1], and HV uses [1,1,1].
% Standard deviations are population standard deviations (ddof=0).

\def\EckerParEGOMean{0.849604}
\def\EckerParEGOStd{0.006130}
\def\EckerLLMBOMean{0.862217}
\def\EckerLLMBOStd{0.00000947}

\def\EckerNHVDelta{0.012613}
\def\EckerNHVRelativeGain{1.48\%}
\def\EckerNHVPairedWins{5/5}

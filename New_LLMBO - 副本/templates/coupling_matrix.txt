You are an expert in lithium-ion battery fast charging optimization.

We are optimizing a two-stage constant-current (CC-CC) charging protocol with three decision variables:
  - I₁ (Phase-1 current): [I1_RANGE] A
  - SOC₁ (switching SOC): [SOC1_RANGE]
  - I₂ (Phase-2 current): [I2_RANGE] A

The battery is [BATTERY_MODEL] with nominal capacity [Q_NOM] Ah, charged from SOC₀=[SOC0] to SOC_end=[SOC_END].

We have three competing objectives (all minimized):
  1. Charging time t_charge(θ) — total time to reach SOC_end
  2. Peak temperature T_peak(θ) — maximum cell temperature during charging
  3. Aging degree L_aging(θ) — lithium-ion inventory loss per cycle

We use a physics-informed Gaussian process with a composite kernel:
  k(θ,θ') = RBF(θ,θ') + γ · ∇Ψ(θ)ᵀ W ∇Ψ(θ')

where Ψ(θ) is the ohmic heat proxy and W is a 3×3 symmetric positive semi-definite coupling matrix.

We need three coupling matrices (W_time, W_temp, W_aging), one per objective. Each matrix captures how the physical gradients ∂Ψ/∂I₁, ∂Ψ/∂SOC₁, ∂Ψ/∂I₂ interact w.r.t. that objective.

Physical reasoning guidelines:
- W_time: Charging time is dominated by I₁ and I₂ (higher current → shorter time). SOC₁ affects time allocation between phases. The I₁-I₂ interaction is important (both contribute to total time).
- W_temp: Peak temperature is most sensitive to I₁ (high current at low SOC generates more heat). The I₁ diagonal element should be largest. SOC₁ moderates the heat distribution between phases.
- W_aging: Aging is driven by high current at high SOC (lithium plating risk). I₂ at high SOC₁ is critical. The SOC₁-I₂ interaction is the most important aging driver.

For each matrix, provide a 3×3 symmetric matrix with non-negative eigenvalues. The rows/columns correspond to [I₁, SOC₁, I₂].

Respond with ONLY a JSON object containing three matrices, no other text:
{"W_time": [[w11,w12,w13],[w21,w22,w23],[w31,w32,w33]], "W_temp": [[...],[...],[...]], "W_aging": [[...],[...],[...]]}
